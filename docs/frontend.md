# frontend.md

# フロントエンド仕様

本ドキュメントはフロントエンドの実装仕様を定義する。

対象は `frontend/` ディレクトリ配下の実装である。

---

# 1. 目的

フロントエンドは以下を目的とする。

* 物件情報入力
* 地図選択
* モデルロード
* 価格予測
* 価格推移表示

学習処理は行わない。

---

# 2. 技術スタック

```text
React
TypeScript
Vite
Leaflet
ONNX Runtime Web
Recharts
```

---

# 3. ディレクトリ構成

```text
frontend/

src/

├── features/
│
│   ├── map/
│   │
│   ├── prediction/
│   │
│   └── model/
│
├── components/
│
├── hooks/
│
├── services/
│
├── types/
│
└── utils/

public/

├── models/
├── metadata/
├── histories/
└── stations/
```

---

# 4. Feature構成

## map

責務

* 地図表示
* 地図クリック
* 地域取得

---

## prediction

責務

* フォーム表示
* リクエスト生成
* 結果表示

---

## model

責務

* モデルロード
* 推論実行
* カテゴリ辞書ロード
* 文字列入力からカテゴリIDへの変換

---

# 5. 画面構成

MVPでは単一画面とする。

---

## HomePage

表示内容

```text
地図

↓

物件入力フォーム

↓

予測年・将来シナリオ

↓

予測結果・価格推移グラフ

↓

参考情報タブ
```

---

# 6. 画面レイアウト

```text
+------------------------------------------------------------+
| 地図                                                       |
+------------------------------+-----------------------------+
| 入力フォーム                 | 予測年・将来シナリオ        |
+------------------------------+-----------------------------+
| 予測結果                     | 価格推移                    |
+------------------------------+-----------------------------+
| 参考情報カード                                             |
| 商業施設 / 駅規模 / 災害リスク / モデル                    |
+------------------------------------------------------------+
```

参考情報タブの詳細な設計は `docs/ui-ux.md` を参照する。

---

# 7. 地図機能

## ライブラリ

```text
Leaflet
```

---

## 地図検索

地図パネルの中央上部に地名検索ボックスを表示する。

ユーザーが地名を入力して検索した場合、Nominatim の forward geocoding を利用して該当地点へ地図を移動する。

検索による地図移動だけでは物件位置を確定しない。物件位置、最寄駅、駅徒歩の更新は地図クリック時に行う。

---

## 対応エリア外

全国47都道府県に対応する。日本国外または都道府県を特定できない場所を地図クリックした場合、古い条件で予測を継続せず、予測結果と将来予測点をクリアする。
沖縄本島を除く離島や、駅マスタ上の最寄駅から大きく離れた地点も、現時点では対応エリア外として扱う。

---

## 取得情報

```typescript
prefecture
municipality
station

lat
lon

station_passenger_log

station_line_count

station_operator_count

station_rank
```

駅規模項目は任意。モデルの `featureOrder` に駅別乗降客数特徴量が含まれる場合、駅マスタJSONに含まれる値からブラウザ側で `effective_station_scale` と `has_station_passenger_data` を計算してONNX入力に渡す。

駅マスタに乗降客数がない駅を選んだ場合は、`stationPassengerLog`、`stationLineCount`、`stationOperatorCount` を `0`、`stationRank` を `unknown` として推論する。`ModelManager` は `stationPassengerLog > 0` の場合だけ `hasStationPassengerData = 1` として扱うため、欠損駅でもONNX入力の特徴量数と順序は変わらない。

---

## 動作

```text
地図クリック

↓

位置取得

↓

逆ジオコーディングAPIで都道府県・市区町村取得

↓

駅マスタCSVとの距離計算

↓

最寄駅決定

↓

フォーム反映
```

最寄駅検索は、緯度経度を単位球面上の3次元座標へ変換したKD-treeを利用する。
索引は都道府県別の駅マスタ配列をキーにキャッシュし、最近傍候補の最終距離を
Haversine距離で算出する。検証スクリプトでは全国の代表地点について全件走査と
結果が一致することを確認する。

周辺施設は学習・前処理側で生成した軽量JSONを読み込む。ズーム10以下では画面上の
グリッド単位かつ施設カテゴリ単位でクラスタ化して件数を表示し、カテゴリごとに色を分ける。
同じ地域の異なるカテゴリは重なりを許容する。件数に応じて円を大きく濃くし、クラスタを
クリックするとその範囲へ拡大する。ズーム11以上では個別施設を表示する。

背景地図にはOpenStreetMapタイルを利用する。施設データはブラウザからOverpass APIへ
直接アクセスせず、学習側でOpenStreetMapデータを取得・キャッシュ・正規化して
`frontend/public/facilities/nearby_facilities.json` として配信する。

---

# 8. 地域入力

地図入力後も編集可能とする。

---

## 入力項目

```text
都道府県

市区町村

最寄駅
```

---

## 最寄駅

検索可能なプルダウンを利用する。

例

```text
大

↓

大宮
大森
大塚
```

---

# 9. 物件入力フォーム

## 数値入力

```text
面積

築年数

駅徒歩

予測年
```

---

## 選択入力

```text
間取り

建物構造
```

建物構造は学習データのカテゴリ表記に合わせ、以下を選択肢に含める。

```text
ＲＣ
ＳＲＣ
鉄骨造
木造
軽量鉄骨造
その他
```

---

## 自動再計算

予測ボタンは表示しない。

フォームの値、地図クリックによる位置、最寄駅、駅徒歩が変わった場合は、フロントエンド側で自動的に価格予測を再実行する。

予測結果と価格推移グラフは、最新の入力値に合わせて再描画する。

駅徒歩は、地図クリックで自動算出された値か、ユーザーが手入力した値かをフォーム上に表示する。

---

# 10. PredictionRequest

推論用DTO。

---

## Interface

```typescript
export type PredictionRequest = {

  prefecture: string;

  municipality: string;

  station: string;

  area: number;

  age: number;

  stationDistance: number;

  roomLayout: string;

  buildingType: string;

  predictionYear: number;
};
```

---

# 11. PredictionResult

推論結果DTO。

---

## Interface

```typescript
export type PredictionResult = {

  predictedPrice: number;

  pricePerSquareMeter: number;

  lowerPrice: number;

  upperPrice: number;
};
```

---

# 12. ModelManager

モデル管理クラス。

---

## 責務

* モデルロード
* モデルバイナリの優先ダウンロード
* バージョン単位のブラウザキャッシュ
* 推論
* 前処理
* カテゴリ辞書ロード
* モデルメタデータロード

---

## Interface

```typescript
class ModelManager {

  loadMetadata()

  loadModel()

  loadCategoryDictionary()

  predict()
}
```

---

## 推論前変換

ONNX Runtime Webへ渡す前に、ModelManagerが文字列入力を数値特徴量へ変換する。

学習時に生成されたカテゴリ辞書JSONを読み込む。

例

```json
{
  "stations": {
    "大宮": 123,
    "新宿": 456
  }
}
```

変換対象

```text
prefecture
municipality
station
roomLayout
buildingType
```

---

# 13. モデルロード

首都圏4都県は都県別モデル、それ以外は8地方モデルから対象モデルをロードする。駅マスタは都道府県単位で分離し、選択された都道府県のファイルだけをロードする。

初期表示では東京都モデルを最優先で読み込む。東京都モデルの準備後、ブラウザがアイドルになったタイミングで、神奈川県、埼玉県、千葉県の順にモデルバイナリだけを取得する。

地図クリックまたは都道府県変更が行われた場合は、低優先度の取得を中断し、選択された地域のモデルを最優先で取得する。データセーバーまたは低速回線ではバックグラウンド取得を行わない。

事前取得したバイナリはCache Storageへ保存する。ONNXセッションは事前生成せず、直近2地域だけをメモリに保持する。

---

## 例

```text
東京都

↓

tokyo_latest.onnx

↓

ロード
```

---

## 保存先

```text
public/models/
```

関連メタデータは以下に配置する。

```text
public/metadata/
public/histories/
public/stations/
public/model-manifest.json
```

価格推移は初期表示用の `*_latest_history.json`（2020年以降）と、操作時にだけ取得する
`*_archive_history.json`（2019年以前）に分割する。初回ロード時の転送量を抑え、過去の実績を
表示する場合だけアーカイブを追加取得する。
アーカイブ表示後は2020年以降の表示へ戻す操作を用意し、過去データを画面から閉じられるようにする。

`model-manifest.json`にはモデルのパス、SHA-256、容量、首都圏の取得優先順を記録する。モデル更新後はバージョン付きURLが変わるため、古いキャッシュと混在しない。

---

# 14. モデル未対応地域

日本国外は選択できない。モデルまたは駅マスタが未配置の場合は予測を行わず、読み込みエラーを表示する。

---

例

```text
日本国外

↓

選択不可
```

---

# 15. 推論フロー

初期表示では東京駅付近にピンを置き、地図でピンを更新した後は条件入力フォームへ自動的に移動する。スマホでは下部フォームを半開にして先頭を表示する。
スマホの下部シートではドラッグハンドルを上端へ固定し、結果や価格推移までスクロールした後も、その場からシートを下げて地図へ戻れるようにする。

```text
フォーム入力

↓

PredictionRequest生成

↓

ModelManagerでカテゴリ変換

↓

ModelManager.predict()

↓

PredictionResult

↓

画面表示
```

---

# 16. 推論シーケンス

```mermaid
sequenceDiagram

    actor User

    User->>Form:
    入力

    Form->>PredictionRequest:
    作成

    PredictionRequest->>ModelManager:
    predict()

    ModelManager->>ModelManager:
    category id conversion

    ModelManager->>ONNXRuntime:
    execute

    ONNXRuntime-->>ModelManager:
    result

    ModelManager-->>Form:
    PredictionResult

    Form-->>User:
    表示
```

---

# 17. 予測結果表示

表示内容

```text
予測価格

平米単価

参考価格帯
```

参考価格帯は、モデルメタデータに検証データの残差分位点がある場合は `予測値 + 残差分位点` で表示する。残差分位点がない既存成果物では、後方互換として予測価格に検証データのMAEを足し引きした目安を表示する。

特徴量重要度を表示する場合は、モデル全体の重要度として扱う。個別予測の厳密なSHAP寄与度ではないことを前提に、UI文言と設計は `docs/model-explainability.md` に従う。

---

## 表示例

```text
予測価格

5,234万円

平米単価

87万円/㎡

参考価格帯

4,900万円
～
5,600万円
```

---

# 18. 価格推移

## ライブラリ

```text
Recharts
```

---

## 表示内容

```text
過去価格推移

現在価格

将来予測
```

過去価格推移は学習データ全件ではなく、学習時に生成した集計済みJSONを読み込む。

MVPでは最寄駅単位・年単位で集計する。

学習データ範囲外の将来年を表示する場合、グラフの中間年は単なる直線補間にしない。

学習最終年のモデル予測値を基点に、駅別または地域別の価格推移トレンドを各年に適用した予測点を表示する。

実績値は最寄駅、5㎡刻みの面積帯、5年刻みの築年帯が近い取引の㎡単価を入力面積へ換算する。対象駅の実績がない年は、半径5km以内の近い12駅までの条件別推移を集計し、学習最終年のモデル予測価格へ水準を合わせた推計値を表示する。学習最終年は予測点を重ねず、最新の実績または推計値を起点として翌年以降へ予測線を伸ばす。

```text
対象駅の類似実績: 実線
周辺実績・モデルによる推計: 短い破線
入力条件の予測: 破線
```

---

## グラフ

同一グラフに表示する。

---

## イメージ

```text
過去

|

|

現在

|

|

将来
```

---

# 19. グラフデータ

## Interface

```typescript
type PriceHistoryPoint = {

  year: number;

  price: number;
};
```

集計済みJSONの例

```json
{
  "station": "大宮",
  "year": 2024,
  "avg_price": 42000000
}
```

学習データ全件をブラウザへ配布しない。

---

# 20. React状態管理

状態管理ライブラリは利用しない。

---

利用可能

```typescript
useState

useMemo

useEffect
```

---

利用しない

```text
Redux

Zustand

MobX
```

---

# 21. エラーハンドリング

## モデルロード失敗

表示

```text
モデルの読み込みに失敗しました
```

---

## 推論失敗

表示

```text
価格予測に失敗しました
```

---

## 必須入力不足

表示

```text
入力内容を確認してください
```

---

## 長期将来予測

学習データの最新年から10年を超える予測年が入力された場合は警告を表示する。

将来年の予測は、LightGBM の transaction_year 特徴量だけでは外挿できないため、学習最終年のモデル予測値に価格推移トレンドを加えて算出する。

駅別価格推移が2点以上ある場合は駅別トレンドを利用し、不足する場合は地域全体の年別平均価格トレンドを利用する。

将来価格をシミュレーション機能へ拡張する場合は、シナリオ切り替えと参考レンジ表示に分ける。詳細は `docs/future-price-simulation.md` に記載する。

表示

```text
長期予測のため精度は保証できません
```

---

## 参考価格の注記

予測結果の近くに、表示価格が参考値であることを明示する。

```text
予測価格は公開取引データに基づく参考値です。実際の査定額や成約価格を保証するものではありません。
```

---

# 22. 駅マスタ

CSV管理とする。

---

## 駅徒歩計算

地図クリック時の最寄駅探索では、駅マスタとの直線距離を Haversine で算出する。

フォームに反映する駅徒歩は距離kmをそのまま使わず、以下で徒歩分に変換する。

```text
徒歩分 = ceil(直線距離km × 1000 / 60)
```

直線距離は実際の徒歩経路より短く出やすいため、MVPでは補正込みの簡易換算として 60m を徒歩1分として扱う。

最低値は1分とする。

---

## 項目

```text
station_id

station_name

prefecture

line_name

lat

lon
```

---

## 最寄駅取得

地図クリック時の緯度経度と駅マスタCSVの緯度経度から距離を計算し、最寄駅を決定する。

最寄駅検索には駅マスタ配列ごとにキャッシュしたKD-treeを利用する。

---

# 23. 参考価格帯

MVPでは評価時に算出した残差分位点を利用し、以下の方式で表示する。

```text
下限 = 予測価格 + 残差の2.5パーセンタイル
上限 = 予測価格 + 残差の97.5パーセンタイル
```

残差分位点がない既存成果物では、後方互換としてモデルメタデータJSONのMAEを利用し、`予測価格 ± MAE` を参考価格帯として表示する。

## 学習範囲外の入力

モデルメタデータの `inputRanges` には、配布モデルの学習行から算出した `area`、`age`、
`stationDistance`、`transactionYear` の最小値・最大値を保持する。いずれかの入力が範囲外の
場合、予測を止めず、予測結果カードの価格より前に対象値と学習範囲を表示する。

面積・築年数・駅徒歩では誤差が大きくなる可能性を示す。予測年が最新学習年より後の場合は、
モデルが未来の取引を直接学習したものではなく、過去の駅別・地域別傾向を使った参考
シミュレーションであることを示す。`inputRanges` がない旧メタデータでは警告判定を行わない。

結果カードの免責は警告の有無にかかわらず常に表示し、査定額、成約価格、将来価格を保証しない。

## 類似取引サンプルの警告

価格推移JSONの最新年を基準に前後2年を対象とし、同じ駅・面積5㎡帯・築年5年帯の取引が
3件以上ある場合を完全一致とする。3件未満なら価格推移表示と同じ順序で築年帯を広げ、
それでも不足する場合は駅全体の件数を表示する。駅の直近集計がない場合も予測自体は継続し、
モデル全体の傾向による参考値であることを価格より前に表示する。

## 条件別のモデル評価

モデルメタデータに `evaluation.segments` がある場合、モデル詳細へ入力中の予測価格帯、
築年数帯、面積帯、都道府県に一致する評価年の件数とMAEを表示する。100件未満の区分は
件数のみを表示し、MAE・MAPEは表示しない。旧メタデータではこの欄自体を表示しない。

区分別残差分位点を参考価格帯へ適用する案は、別年評価を行うまで採用しない。現行の参考価格帯は
引き続きモデル全体の残差分位点を利用する。

---

# 24. 将来対応

## GeoJSON

外部APIを利用しない地域判定。

---

## 全国対応

首都圏4都県の専用モデルと8地方モデルを用いて対応済み。モデル地域と都道府県別駅マスタを分離し、地図操作された地域のモデルを優先取得する。

---

## 商業施設表示

将来的な追加候補。

商業施設データを表示する場合は、学習側で生成した配信用JSONだけを `frontend/public/facilities/` から読み込む。

地図上の周辺施設とハザードは、デスクトップ・スマートフォンともフローティングボタン
から開閉する。開いているパネルは一つだけとし、スマートフォンではボトムシート風の
角丸パネルとして表示する。

周辺施設パネルは、商業施設を規模別、美術館・博物館を「美術館・ギャラリー」と
「博物館・資料館」、温浴施設を「銭湯」「スーパー銭湯・スパ」「温泉・その他」、
映画館を「シネコン」「ミニシアター・その他」のチェックボックスで絞り込む。
細分類の元データがない施設は名称から分類し、判別できないものを必ず「その他」へ
残して非表示にならないようにする。

ブラウザから日本ショッピングセンター協会（JCSC）サイトへ直接アクセスしない。

ハザード情報は安全性の参考として、価格モデルへの採否と分けて公式ラスタレイヤーを表示する。
商業施設も価格モデルへの採否と分け、座標を確認できた全国2,911件を地図マーカーと
周辺施設情報として表示する。店舗面積またはテナント数から4段階の規模を付与し、
規模不明を別区分として残す。

2026-07の首都圏4都県475,292件の比較では、洪水・土砂災害特徴量をPOI特徴量へ
追加しても精度改善がなかった。このため価格補正には使用せず、公式ラスタレイヤー、
出典リンク、免責文を安全性の参考情報として維持する。学習用の代表座標とユーザーが
選択した任意地点は一致しないため、代表座標の数値判定を近傍地点へ流用しない。

商業施設の参考情報カードでは、市区町村単位の集計に加えて周辺施設の名称・距離・規模を表示する。

表示項目:

```text
市区町村内SC件数
直近開業SC
店舗面積合計
テナント数合計
データ更新年
出典
```

地図マーカーと距離表示には信頼できる緯度経度を持つ施設だけを利用し、町丁目・自治体の
代表点だけで補完した座標は採用しない。

---

## 画面優先順位

ユーザーが最初に確認したい情報は、入力フォーム、予測結果、価格推移とする。

デスクトップ/モバイルともに、地図の次に次の順で表示する。

```text
入力フォーム
↓
予測結果
↓
価格推移
↓
周辺参考情報
↓
災害リスク評価
↓
条件詳細・モデル詳細
```

災害リスク評価、商業施設などの周辺参考情報、条件詳細、モデル詳細は、価格予測の主動線を邪魔しない位置へ置く。モデル詳細や特徴量重要度は初期状態では折りたたみ、必要なユーザーだけが開く。

---

# 25. 非目標

MVPでは実施しない。

```text
ユーザー管理

ログイン

会員登録

お気に入り

予測履歴保存

アクセス解析

SNS機能
```

---

# 26. 完了条件

以下を満たしたらフロントエンド完成とする。

* 地図が表示できる
* 地域取得できる
* 入力できる
* モデルロードできる
* 推論できる
* 価格表示できる
* グラフ表示できる
* GitHub Pagesで動作する
