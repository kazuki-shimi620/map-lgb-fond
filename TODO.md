# TODO

## 作業優先度

優先度は以下の順で扱う。

```text
P0 = 先に片付ける。モデル再生成を伴わず、実装・検証の土台になるもの
P1 = 次に進める。外部特徴量やUIの実装だが、モデル成果物更新は伴わないもの
P2 = モデル更新時にまとめて実施するもの
P3 = 将来拡張または検証後に着手するもの
```

当面はP0/P1を優先する。ONNX再生成、`frontend/public/models`、`frontend/public/metadata`、`frontend/public/stations` の成果物反映、`make train-all PUBLISH_POLICY=latest` はP2として後でまとめて実施する。

コミットはTODOの細かい単位で行う。TODO以外の仕様書・実装ファイルは、関連するTODO単位で先にコミットしてよい。

## 全国モデル配信

* [ ] 全国ベースと地域補正モデルの精度と容量を比較する

## 外部特徴量の導入

### P1 データ更新

* [ ] `make collect-sc-all` で2015年以降のJCSCオープンSC一覧を再取得する
* [ ] `make collect-station-passengers` で駅別乗降客数を再取得する
* [ ] `make collect-data` が不動産CSV、JCSC、駅別乗降客数を意図通り一括更新できるか確認する
* [ ] `make stations` で首都圏4都県の駅マスタJSONを駅規模特徴量付きで再生成する
* [ ] `make stations-national` を実行する場合、全国駅マスタに首都圏以外の駅規模欠損がどの程度出るか確認する

### P1 ショッピングセンター特徴量

* [ ] `make compare-commercial-features` の結果を確認し、採用する商業施設特徴量セットを決める
* [ ] 商業施設特徴量を通常学習パイプラインへ接続する
* [ ] 商業施設特徴量を4都県モデル、全国8地方モデル、全国ベース補正モデルのどこまで入れるか比較する
* [ ] 商業施設の市区町村単位特徴量だけで十分か、緯度経度が取れる場合の距離特徴量を追加するか判断する
* [ ] 商業施設データの配信用JSONまたはGeoJSONを作るか判断する
* [ ] フロントで商業施設を地図表示する場合、`frontend/public/facilities/` から静的に読む実装を追加する

### P1 駅規模特徴量

* [ ] `make compare-station-passenger-features` の結果を最新データで再確認する
* [ ] 駅名一致だけで乗降客数を結合している箇所を、駅マスタ座標を使った同名駅の disambiguation へ改善する
* [ ] フロントで駅規模特徴量が欠損する駅を選んだ場合の推論結果を確認する

### P2 モデル更新時にまとめて実施

* [ ] 4都県モデルを `station` カテゴリなし + 駅規模特徴量ありで再学習する
* [ ] `make train-all PUBLISH_POLICY=latest` 後のONNXサイズ、カテゴリ辞書サイズ、MAEを確認する
* [ ] 採用モデルを `frontend/public/models`、`frontend/public/metadata`、`frontend/public/stations` に反映する
* [ ] 採用モデルの `featureOrder` とフロント推論入力が一致していることを確認する
* [ ] 軽量モデル候補をブラウザで実測し、初回ロード時間と推論時間を確認する
* [ ] 首都圏以外の地方モデルにも駅規模特徴量を入れるか、データ取得範囲と容量を見て判断する

### P2 モデル比較と採用判断

* [ ] ベースライン、商業施設のみ、駅規模のみ、商業施設 + 駅規模の4系統でバックテストする
* [ ] 2015〜2025年に絞った学習と2005〜2025年学習を比較する
* [ ] `station` カテゴリありとなしで、精度差と配布サイズ差を比較する

### P3 今後の拡張候補

* [ ] ハザードマップと商業施設、駅規模を組み合わせた特徴量の候補を整理する
* [ ] LightGBM単体と、地域補正やハイブリッドモデルの比較方針を作る
* [ ] 将来の外部特徴量追加をFeatureProvider方式に寄せて整理する
* [ ] 外部データ更新から学習、成果物反映までの手順をREADMEまたはdocsへまとめる

### P1 ハザードマップ・災害リスク評価

* [ ] 物件緯度経度とハザード情報を結合する前処理を実装する
* [ ] ハザード情報を価格モデルに入れる場合のバックテスト候補を作る

## P3 モデル説明機能

* [ ] SHAP導入検討

予測価格の寄与度表示。

```text
駅徒歩8分 → +800万円
東京駅 → +2500万円
築15年 → -400万円
55㎡ → +1200万円
```

## P3 将来的な改善

* [ ] 学習自動化
* [ ] API取得からGitHub Pagesデプロイまでの一括自動化
* [ ] データ量を増やす、県と年度
* [ ] 周辺特徴量の追加

候補:

```text
商業施設
大学
病院
大型公園
再開発エリア
```

* [ ] 外れ値処理、築古、駅遠、高級物件の補正改善
* [ ] 十年後予測をシミュレーション機能として進化させる

理想フロー:

```text
API取得
↓
前処理
↓
学習
↓
ONNX出力
↓
GitHub Pagesデプロイ
```

- [ ] ノルウェーなどにピンを刺すと市町村が入力できてしまう。日本国外の場合はちゃんとエラーが出ないようにバリデーションを追加する
