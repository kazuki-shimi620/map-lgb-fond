# TODO

## 作業方針

P0/P1を優先する。ONNX再生成、`frontend/public/models`、`frontend/public/metadata` の成果物反映は、モデル採用対象が固まった段階でまとめて実施する。

有料APIや再配布条件が不明なデータは使わず、無料の公式APIまたはオープンデータを優先する。APIキーが必要な場合もブラウザから直接呼ばず、学習側の収集処理に閉じ込める。

完了履歴はgit logと `docs/` に残し、このファイルには未完了・判断待ちの作業だけを置く。

優先度:

- `P0`: 現行機能の品質・本番モデル更新に直結する作業
- `P1`: 次に実装する価値が高く、着手条件が揃っている作業
- `P2`: データ取得や比較結果を待って実施する作業
- `P3`: 将来候補または費用対効果を確認してから着手する作業

## P0/P1 次に進める作業

### モデル・データ

### 公園データ

- [ ] `P3` 大型公園特徴量用に面積またはway/relation面積を集計する
- [ ] `P3` Overpassの504を避けるため、小bbox分割で首都圏全域の `park_areas.csv` を生成する

### 参考情報・地図で一覧表示したい施設

施設ごとに名称、現在地からの距離、住所を基本情報として表示する。営業時間、料金、施設規模などは信頼できる再配布可能なデータがある場合だけ追加する。

優先候補:

- [x] `P1` 映画館・温浴施設を都道府県単位のキャッシュ付きOSM取得で全国化する
  - [x] 日本行政区域に限定した全国OSM映画館520要素を取得・キャッシュする（外接矩形の1,171要素は国外混入のため不採用）
  - [x] 全国OSM映画館520要素と公式施設総数の差をcoverage metadataへ記録し、全数データと誤認させない
  - [x] 地図用JSONを全国映画館520件へ切り替え、首都圏版との重複をOSM IDで除去する
  - [x] 全国の公衆浴場node 2,305要素・天然温泉267要素を取得し、首都圏way/relation分と統合する
  - [x] 地図用JSONを全国の温泉・入浴施設2,969件へ切り替える（首都圏外2,055件）
  - [x] 映画館は大手チェーン公式一覧を優先取得し、5スクリーン以上または大型商業施設併設館を重点対象にする
    - [x] イオンシネマ、TOHOシネマズ、ローソン・ユナイテッドシネマの一覧ページ3件をキャッシュし、営業中計212館を共通CSV化
    - [x] 松竹25館、109シネマズ20館、T・ジョイ20館、シネマサンシャイン17館を追加し、7系列計294館を共通CSV化
  - [x] 公式名称・併設施設名をOSM、JCSC商業施設の順で一括照合し、施設単位のWeb検索を避ける
  - [x] 大手3チェーンの照合・coverage report生成処理を実装する（全国OSM確定後に件数を確定）
  - [x] 公式一覧の未照合分から、5スクリーン以上または大型商業施設併設館だけを優先レビューCSVへ抽出する
  - [x] 優先レビューCSVの大型館だけ住所・座標を補完する
    - [x] 都道府県の空白を正規化し、OSM・JCSCの機械照合で10館を追加補完する
    - [x] Nominatim住所検索をキャッシュ・再開・dry-run付きで実装する
    - [x] 残る25館をNominatim・JCSC・公式地図リンクで補完する
- [ ] `P2` 図書館
- [x] `P1` 美術館・博物館を全国取得し、地図カテゴリとして表示する
  - [x] OSMの美術館・博物館・ギャラリーを地方別グリッドで取得し、地図カテゴリへ7,269件を反映する
  - [x] キャッシュ再実行で失敗セルを115件から0件まで削減する
  - [x] Overpass APIで失敗したセルをキャッシュ再実行し、全国の取得漏れを補完する
- [ ] `P3` 大型公園

追加候補:

- [ ] `P2` 市役所・区役所
- [ ] `P3` 道の駅

## P2 モデル更新時にまとめて実施

- [x] `P2` 商業施設特徴量をハザード特徴量と組み合わせて再評価する
  - [x] 東京3万件・3 seedで商業施設、映画館、美術館・博物館、温浴施設の距離・件数特徴量をdry-runする
  - [x] 首都圏4都県475,292件・2023〜2025年holdoutでPOI特徴量を比較する
  - [x] 首都圏4都県の複数holdoutでPOI単独、ハザード単独、POI+ハザードを比較する
- [x] `P2` 首都圏4都県4,325地点の洪水・土砂災害特徴量を作成し、ハザード特徴量を再比較する
  - 全475,292件ではハザード単独のMAE改善は491円、RMSEは74円悪化
  - POI+ハザードはPOI単独と同値で、現行2特徴量の本番モデル採用は見送る
- [x] `P2` ハザード特徴量は価格モデルへ採用せず、公式ラスタレイヤーと出典リンクを安全性の参考表示として維持する
- [ ] `P2` 不動産情報ライブラリ `XKT013` の将来人口変化率を比較候補としてFeatureProviderへ追加する
  - [x] 首都圏4,325地点をz=15の2,792タイルへ重複排除するdry-runを追加する
  - [x] 1タイル取得で32,228バイト、5メッシュ、2020〜2070年の5年刻み、総人口欠損0件を確認する
  - [x] キャッシュ・再開・再試行・失敗記録付きの取得処理を実装し、1タイルで再開を確認する
  - [ ] 2030年・2040年人口変化率を物件座標へ結合し、時系列holdoutで比較する

## P3 将来的な改善

- [ ] `P3` 取引データの対象都道府県と年度を増やす
- [ ] `P3` 大型公園特徴量の取得・比較
- [ ] `P3` 病院特徴量の取得・比較
- [ ] `P3` 大学特徴量の取得・比較
- [ ] `P3` 再開発エリア特徴量の手動マスタを検討する
