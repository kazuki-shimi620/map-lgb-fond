# TODO

## 作業優先度

優先度は以下の順で扱う。

```text
P0 = 先に片付ける。モデル再生成を伴わず、実装・検証の土台になるもの
P1 = 次に進める。外部特徴量やUIの実装だが、モデル成果物更新は伴わないもの
P2 = モデル更新時にまとめて実施するもの
P3 = 将来拡張または検証後に着手するもの
```

当面はP0/P1を優先する。ONNX再生成、`frontend/public/models`、`frontend/public/metadata`、`frontend/public/stations` の成果物反映、`make train-all PUBLISH_POLICY=latest` はP2として後でまとめて実施する。

有料API、有料契約前提、料金不明、またはキャッシュ・再配布条件が曖昧な外部サービスは使わず、P3または調査保留へ回す。無料の公式APIやオープンデータだけを優先し、APIキーが必要な無料APIもブラウザから直接呼ばず学習側の取得処理に閉じ込める。

コミットはTODOの細かい単位で行う。TODO以外の仕様書・実装ファイルは、関連するTODO単位で先にコミットしてよい。

## コード品質・運用安全性の改善

### P0 優先修正

* [x] UIとドキュメントの「信頼区間」を「参考価格帯」へ変更し、`予測価格 ± 検証データMAE` は統計的な信頼区間ではないことを明示する
* [x] 特徴量重要度のUI文言を「モデル全体の特徴量重要度」に変更し、個別予測への寄与と誤解されにくくする
* [x] `ModelManager` で `featureOrder` に未対応の特徴量が来た場合は `0` で埋めず、例外を投げてfail-fastにする
* [x] `App.tsx` から `usePropertySelection` を切り出し、逆ジオコーディング、対応地域判定、駅マスタ取得、最寄駅計算、フォーム更新を集約する
* [x] `useRegionAssets` を切り出し、モデル、メタデータ、カテゴリ辞書、駅マスタ、価格履歴、商業施設などの読み込み責務をAppから分離する
* [x] 地域アセット読み込みを `modelStatus`、`stationStatus`、`historyStatus`、`facilityStatus`、`hazardStatus` に分け、予測可能な縮退運転と周辺情報の部分失敗をUIで区別する

### P1 分割・テスト強化

* [x] `usePricePrediction` を切り出し、予測実行、モデル切り替え、将来予測、エラー処理をAppから分離する
* [x] `usePriceHistory` を切り出し、履歴読み込み、アーカイブ履歴、将来価格シミュレーション用ポイント生成をAppから分離する
* [x] `useMobilePredictionSheet` を切り出し、モバイルシート状態とスクロール処理をAppから分離する
* [x] Python CIを追加し、構文チェック、Ruff、前処理/FeatureProvider/カテゴリ辞書生成を含む単体テストを段階的に実行する
* [x] Python CIへ型チェックを追加する
* [x] Ruffの段階導入で一時的に除外している `E501`、`E402` を解消し、Lint対象へ戻す
* [x] Python成果物とTypeScript推論入力の契約テストを追加し、`metadata.featureOrder`、カテゴリ辞書、`unknownId`、ONNX入力次元の整合性を検証する

### P2 モデル成果物更新時に検討

* [x] 本当の予測区間を表示する場合は、検証データの残差分位点をメタデータへ出力し、下限/上限を `予測値 + 残差分位点` で算出する
* [x] 個別予測への寄与を表示する場合は、SHAP値または学習時に生成した代表的な説明情報を配信用メタデータとして設計する
* [x] 意図的に0補完できる特徴量が必要になった場合は、TypeScript側の暗黙補完ではなくメタデータにdefault値を定義する

## 外部特徴量の導入

### P0 UI動線の見直し

* [x] デスクトップ/モバイルの表示順を「地図 → 入力フォーム → 予測結果 → 価格推移」に整理する
* [x] 災害リスク評価、条件詳細、モデル詳細、特徴量重要度を価格推移より下へ移動する
* [x] `PredictionResultView` の主要結果と条件・モデル詳細を分離し、詳細は初期状態で折りたたむ
* [x] UI変更後にPlaywright E2EとREADME用スクリーンショット仕様を更新する

### P1 周辺情報の画面表示

* [x] 商業施設情報を価格モデル採用とは分けて、参考情報カードとして表示する方針を仕様化する
* [x] JCSCデータから市区町村/都道府県単位の配信用軽量JSONを生成する
* [x] 物件所在地に対応する「周辺商業施設」カードを追加する
* [x] 商業施設カードには市区町村内SC件数、直近開業SC、店舗面積合計、データ更新年、出典を表示する
* [ ] 商業施設に緯度経度を付与できるようになったら、最寄SC距離と地図マーカー表示を追加する
* [x] 駅規模情報を参考情報として表示するか検討する

### P2 モデル更新時にまとめて実施

* [ ] 首都圏以外の地方モデルにも駅規模特徴量を入れる場合、全国範囲の駅別乗降客数を取得して再評価する
* [x] 駅規模特徴量を入れる場合も `station` カテゴリは残し、駅名固有の地価・沿線・立地情報を落とさない構成で再学習する
* [x] `2005-2025` と `2015-2025` の学習期間を比較し、近年データに絞った方が駅規模・商業施設・再開発影響を拾いやすいか検証する
* [x] 首都圏4県で `station` + 駅規模特徴量の組み合わせを先に比較し、MAE/RMSE/MAPEとONNXサイズの変化を記録する

### P2 モデル比較と採用判断

* [ ] 商業施設特徴量をハザード特徴量と組み合わせる比較時に再評価する
* [x] ハザードCSVがある場合に `make compare-external-features` へ `hazard` / `commercial_station_hazard` 候補を含める
* [x] 商業施設特徴量はまず首都圏4県に限定して、駅規模特徴量と同時投入/単独投入の差を比較する
* [ ] ハザード特徴量は商業施設特徴量の比較後に追加し、精度改善よりもリスク説明力・表示価値も含めて採用判断する
* [x] `has_station_passenger_data` のように重要度がほぼ出ないフラグ特徴量は、削除候補として比較する
* [x] 次回モデル更新では `train_start_year: 2015` と `has_station_passenger_data` 削除を本番候補として試す
* [x] `train_start_year` 候補を2023〜2025年など複数holdout年で比較できる評価ターゲットを追加する
* [ ] 2015年開始はバックテストでは良いが2025年holdoutで微悪化したため、採用前に複数holdout年で再検証する

### P1 現行仕様に合わせたコード整理

* [x] `Makefile` の `collect` / `collect-all` を不動産価格API取得ではなく legacy 検証用へ退避、または削除する
* [x] `Makefile` のhelpから不動産価格API取得の案内を消し、公式画面CSV取得だけを主経路として表示する
* [x] `training/src/collect/mlit.py` の `REINFOLIB_API_KEY_ENV` を駅別乗降客数などの共通定数へ移し、駅規模取得が不動産価格API実装に依存しないようにする
* [x] `training/src/collect/collect.py` と `training/src/collect/mlit.py` を削除するか、`legacy` 配下へ移して通常導線から外す
* [x] `training/tests/test_collect_mlit.py` を削除または legacy 検証用へ移し、通常テストの主対象から外す
* [x] `model-update-background` の多重起動を検出し、SQLite `database is locked` を起こす前に分かりやすく終了する

### P2 追加特徴量の比較候補

* [x] 住みやすさ・将来性データソースを難易度別に整理し、取得スクリプト設計を `docs/data-source-acquisition.md` にまとめる

#### 地価公示・基準地価

* [x] 不動産情報ライブラリ `XPT002` / `XCT001` のAPI仕様、パラメータ、レスポンス項目、利用条件を確認する
* [x] `training/src/collect/land_prices.py` の設計を固め、raw/processed/cache/error logの出力形式を決める
* [x] 地価公示・基準地価collectorを実装する
* [ ] `make collect-land-prices` で首都圏4県の地価ポイントCSVを生成する
* [x] 地価ポイントから市区町村平均、前年比、最寄地価ポイント距離を生成するFeatureBuilderを実装する
* [x] `FeatureProvider` に地価特徴量を追加し、欠損時の `has_land_price_data` を明示する
* [x] `make compare-land-price-features` で地価特徴量の単独効果と `station` なし構成を比較できるようにする
* [ ] 公示地価/基準地価を取得し、地価水準と地価変化率を比較する

#### 人口・世帯数・年齢構成

* [x] e-Stat APIで使う統計表ID、地域コード、年度、取得粒度を調査し、自治体単位の初期仕様を決める
* [x] `training/src/collect/population_stats.py` の設計を固め、人口・世帯数・年齢構成の正規化CSVスキーマを決める
* [x] 自治体人口統計CSVを正規化し、人口、世帯数、人口密度、高齢化率、生産年齢人口比率を生成するcollectorを実装する
* [x] e-Stat APIから自治体人口統計CSVを直接生成するcollectorを実装する
* [x] 不動産情報ライブラリ `XKT013` の将来推計人口250mメッシュを使うか、自治体単位に留めるか比較方針を決める
* [x] `FeatureProvider` に人口統計特徴量を追加し、人口変化率を比較候補にする
* [x] `make compare-population-features` で人口統計特徴量の単独効果と `station` なし構成を比較できるようにする
* [ ] 将来人口変化率を比較候補にする場合は `XKT013` との接続後にFeatureProviderへ追加する
* [ ] 人口・世帯数・人口密度・年齢構成を自治体単位で比較する

#### 路線利便性・ターミナル駅フラグ

* [x] ターミナル駅リストと主要目的地駅リストを自前JSON/CSVとして設計する
* [x] Google Routes API、駅すぱあと、NAVITIMEは事前計算用途に限定し、料金・商用利用・キャッシュ可否を確認する
* [x] `training/data/manual/rail/terminal_stations.csv` と `major_station_travel_times.csv` の初期fixtureを作る
* [x] `training/src/collect/rail_access.py` の設計を固め、リアルタイムAPIなしで特徴量CSVを生成できるようにする
* [x] `FeatureProvider` に主要駅所要時間、最短ターミナル所要時間、ターミナル駅フラグを追加する
* [x] `make compare-rail-access-features` で路線利便性特徴量の単独効果と `station` なし構成を比較できるようにする
* [x] 路線利便性として主要駅までの所要時間、乗換回数、ターミナル駅フラグを比較する

#### 学区・保育園・学校距離

* [x] 不動産情報ライブラリ `XKT004` / `XKT005` / `XKT006` / `XKT007` のAPI仕様、属性名、年度、ジオメトリ種別を確認する
* [x] `training/src/collect/education_facilities.py` の設計を固め、学校区ポリゴンと施設ポイントの出力形式を分ける
* [x] 学校・保育園collectorを実装し、小学校、中学校、保育園、幼稚園の正規化CSVを生成する
* [x] 取引地点または選択地点から最寄学校距離、半径500m/1km件数を生成するFeatureBuilderを実装する
* [x] 初期はモデル特徴量ではなく参考情報カードとして表示するか判断する
* [x] 学区、保育園、学校距離を使う場合のデータ取得可否とノイズリスクを確認する

#### 犯罪・治安

* [x] 都道府県警、市区町村オープンデータ、e-Statの候補を調査し、全国統一可否と粒度差を整理する
* [x] 犯罪・治安指標は星評価にせず、人口1000人あたり刑法犯認知件数、出典、年度、集計単位を表示する方針を仕様化する
* [x] `training/src/collect/crime_stats.py` の設計を固め、自治体単位fixtureで正規化CSVスキーマを検証する
* [x] 人口統計データと結合し、人口あたり犯罪件数を算出するFeatureBuilderを実装するか判断する
* [x] 犯罪・治安指標を使う場合の出典、粒度、表示上の注意点を確認する

#### 用途地域・都市計画

* [x] 不動産情報ライブラリ `XKT001` / `XKT002` / `XKT003` のAPI仕様と属性名を確認する
* [x] `training/src/collect/urban_planning.py` の設計を固め、用途地域ポリゴンを取引地点へ付与する方法を決める
* [x] 用途地域collectorを実装し、用途地域、建ぺい率、容積率、区域区分の正規化CSVを生成する
* [x] `FeatureProvider` に用途地域特徴量を追加し、価格モデル特徴量と参考表示の両方で比較する

## P3 将来的な改善

* [x] GitHub Actionsの手動実行から成果物更新PRを作る仕組みの検討
* [ ] データ量を増やす、県と年度
* [ ] 大型公園特徴量の取得・比較
* [ ] 病院特徴量の取得・比較
* [ ] 大学特徴量の取得・比較
* [ ] 再開発エリア特徴量の手動マスタ検討

* [x] エッジケース集計を元に外れ値処理候補をバックテストする
* [x] 将来価格シミュレーション用のトレンド集計JSONを生成する
* [x] 将来価格シミュレーションのシナリオ切り替えUIを追加する

理想フロー:

```text
ローカルまたは手動workflowでデータ取得
↓
前処理
↓
学習
↓
ONNX出力
↓
成果物更新PR
↓
mainマージ後にGitHub Pagesデプロイ
```
